<div class="flex flex-col gap-2 justify-center">
    <% @workouts.each do |w|%>
        <div class="bg-card px-4 py-2 rounded-xl">
        <%= link_to w.name, workout_path(w) %>
        </div>
    <% end %>
</div>