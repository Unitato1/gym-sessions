<div class="flex gap-4">
    <%= link_to "Edit", edit_exercise_path(exercise), class: "underline border-1 rounded-lg p-1" %>
    <%= button_to "Delete", exercise, method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "hover:cursor-pointer border-1 p-1 rounded-md" %>
</div>