<h1 class="bg-card bg-card p-4 m-2">All workouts</h1>
<div class="flex flex-col gap-2 justify-center m-2">
    <% @workouts.each do |workout|%>
      <%= render "workout", workout: workout %>
    <% end %>
</div>