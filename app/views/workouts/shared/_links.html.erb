<% if current_user == workout.user %>
    <div class="flex gap-4 bg-card rounded-md my-2 p-2">
        <%= link_to "Edit", edit_workout_path(workout), class: "underline"%>
        <%= button_to "Delete", workout, method: :delete, data: { turbo_confirm: "Are you sure?" }, class: "underline hover:cursor-pointer" %>
    </div>
<% end %>