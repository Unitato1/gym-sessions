<h1 class="bg-card bg-card p-4 m-2">All exercises</h1>
<div class="flex flex-col gap-2">
    <% @exercises.each do |e| %>
      <div class="bg-card p-4 mx-2 rounded-2xl">
          <%= render "exercise", exercise: e %>
      </div>
    <% end %>
</div>