<%= link_to exercise.name, exercise, class: "underline" %>
<%= exercise.description %>
<%= link_to "@#{exercise.user.email.split('@').first}", user_path(exercise.user.id), class: "text-gray-500 text-sm" %>
